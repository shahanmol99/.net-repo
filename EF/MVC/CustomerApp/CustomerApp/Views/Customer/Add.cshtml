
@{
    ViewBag.Title = "Add";
    Layout = "~/Views/Customer/MasterPage.cshtml";
}

@model CustomerApp.Models.AddViewModel

<div class="container">
    <div class="row col-md-6 col-md-offset-3">
        <div class="panel panel-primary">
            <div class="panel-heading">
                <h1>Add FORM</h1>
            </div>

            @using (Html.BeginForm())
            {
                <h2></h2>
                <form style="width: 450px; margin: auto;">

                    <div class="form-group">
                        <label for="name">Name</label>
                        @Html.TextBoxFor(m => m.Name)
                        <span style="color:red">@Html.ValidationMessageFor(m => m.Name) </span>
                    </div>

                    <div class="form-group">
                        <label for="name">Location</label>
                        @Html.TextBoxFor(m => m.Location)
                        <span style="color:red">@Html.ValidationMessageFor(m => m.Location) </span>
                    </div>

                    <div class="form-group">
                        <button class="btn btn-primary">Submit</button>
                    </div>
                </form>
            }
        </div>
    </div>
</div>

